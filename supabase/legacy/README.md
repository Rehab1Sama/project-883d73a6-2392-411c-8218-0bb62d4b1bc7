# ملفات SQL تاريخية (مرجعية فقط)

هذه الملفات أُعيد بناؤها قبل الاستيراد ولا تطابق حالة قاعدة البيانات الحالية
(القاعدة الحية تستخدم دوال أحدث مثل `can_manage_circle_entries` بدل
`can_manage_progress_entries`). لا تُطبَّق ولا تُشغَّل تلقائيًا.

الهجرات الفعّالة موجودة في `supabase/migrations/` وآخرها
`20260901000000_student_portal.sql` (بوابة الطالبة) وهي مطبَّقة على القاعدة.
