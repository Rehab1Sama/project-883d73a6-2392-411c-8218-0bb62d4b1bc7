-- =====================================================================
-- السماح لزوار الصفحة العامة (anon) بمعرفة الميزات الفعلية لمقرأة معيّنة
-- =====================================================================
-- صفحة المقرأة العامة (m/[slug]) تحتاج تتحقق من ميزة "الصفحة التعريفية"
-- (public_page) قبل عرض المحتوى الحر اللي تكتبه القائدة، بدل ما تشتغل
-- دائمًا بلا شرط. tenant_effective_features() كانت ممنوحة فقط لـ
-- authenticated، فزوار الصفحة العامة (anon) ما يقدرون يستدعونها.
--
-- الدالة SECURITY DEFINER وstable وتُرجع فقط feature_key/enabled لمقرأة
-- محددة (بلا أي بيانات حساسة) — نفس نوع المعلومة المكشوفة أصلاً عبر
-- أعمدة عامة أخرى بجدول tenants مثل registration_open وvolunteering_open،
-- فمنحها لـ anon لا يكشف شيئًا إضافيًا.
-- =====================================================================

grant execute on function public.tenant_effective_features(uuid) to anon;
